{
  "devDependencies": {
    "markdown-it-any-block": "^3.2.9",
    "vitepress": "^1.3.4"
  },
  "scripts": {
    "docs:dev": "vitepress dev --host 0.0.0.0 --port 8080",
    "docs:build": "vitepress build",
    "docs:preview": "vitepress preview --host 0.0.0.0 --port 8080"
  }
}